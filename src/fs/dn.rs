use crate::meta::types;

pub struct Manager {
    size: usize,
}

impl Manager {
    fn new(size: usize) -> Manager {
        Manager{size}
    }

    fn download(blocks: &Vec<types::FileBlock>)  {

    }
}